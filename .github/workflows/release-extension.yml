name: Release extension zip

on:
  push:
    branches:
      - develop
      - main
      - master
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: latest-extension-release
  cancel-in-progress: false

jobs:
  release:
    name: Build and publish latest release
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: sidebar/package-lock.json

      - name: Build AI sidebar
        working-directory: sidebar
        run: |
          npm ci
          npm run build

      - name: Install zip
        run: |
          sudo apt-get update
          sudo apt-get install -y zip

      - name: Package extension
        id: package
        shell: bash
        run: |
          VERSION="$(node -p "require('./manifest.json').version")"
          SHORT_SHA="$(git rev-parse --short HEAD)"
          ZIP_NAME="adblock-extension-latest.zip"
          STAGING_DIR="$(mktemp -d)"
          EXT_DIR="$STAGING_DIR/adblock-extension"

          mkdir -p "$EXT_DIR"

          for path in \
            manifest.json \
            background.js \
            content.js \
            ai-content.js \
            blacklist.json \
            icons \
            blocking \
            fullpage-capture \
            phishing-alert \
            pip \
            popup \
            rules \
            sidebar/dist \
            studocu \
            study-blocker
          do
            cp -R "$path" "$EXT_DIR/"
          done

          (cd "$STAGING_DIR" && zip -qr "$GITHUB_WORKSPACE/$ZIP_NAME" adblock-extension)

          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "short_sha=$SHORT_SHA" >> "$GITHUB_OUTPUT"
          echo "zip_name=$ZIP_NAME" >> "$GITHUB_OUTPUT"

      - name: Move latest tag to this commit
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git tag -f latest "$GITHUB_SHA"
          git push --force origin refs/tags/latest

      - name: Create or update latest release
        env:
          GH_TOKEN: ${{ github.token }}
          ZIP_NAME: ${{ steps.package.outputs.zip_name }}
          VERSION: ${{ steps.package.outputs.version }}
          SHORT_SHA: ${{ steps.package.outputs.short_sha }}
        run: |
          NOTES="Automatic extension build from commit ${GITHUB_SHA} (manifest v${VERSION}, ${SHORT_SHA}). Download ${ZIP_NAME}, extract it, then load the extracted adblock-extension folder in Chrome."

          if gh release view latest >/dev/null 2>&1; then
            gh release edit latest \
              --title "Latest AdBlock Extension" \
              --notes "$NOTES"
          else
            gh release create latest \
              --title "Latest AdBlock Extension" \
              --notes "$NOTES"
          fi

          gh release upload latest "$ZIP_NAME" --clobber
